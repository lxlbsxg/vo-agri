# Copy this file to .env and fill in a real value. .env is gitignored —
# never commit real secrets.

# Used to sign login session cookies. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
